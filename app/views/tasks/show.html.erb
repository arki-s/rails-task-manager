<div class="container">
  <h1><%= @task.title %></h1>
  <p></p>
  <h2><%= @task.details %></h2>
  <p></p>
  <% if @task.completes.find_by(date: Date.today).status == false %>
    <h2>Done today?</h2>
    <%= render 'completes/done', complete: @complete %>
  <% elsif @task.completes.find_by(date: Date.today).status == true %>
    <h2>Great! Already done today!</h2>
  <% elsif @task.completes.find_by(date: Date.today).nil? %>
    <h2>This task has been created for over 30 days. If you want to keep a new record, please create a new task.</h2>
  <% end %>
  <%# <% end %>
  <div class="checks">
    <% @task.completes.each do |complete| %>
      <% if complete.status == true %>
        <%# <%= complete.date %>
        <div class="check-done"></div>
      <% else %>
        <%# <%= complete.date %>
        <div class="check-none"></div>
      <% end %>
    <% end %>
  </div>
  <%# <% if @task.completed %>
  <%# <h3><%= "This task is completed! 🌟" %>
  <%# <% else %>
  <%# <h3><%= "This task is not completed yet. 🤪" %>
  <%# <% end %>
  <p></p>
  <div class="trash">
    <%= link_to "", edit_task_path(@task), class:"fa-solid fa-pen p-2" %>
    <%= link_to "", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Delete #{@task.title}?"}, class:"fa-solid fa-trash-can p-2" %>
  </div>
  <%= link_to "Back to List", tasks_path %>
</div>
